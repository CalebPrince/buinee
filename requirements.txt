openpyxl

# Encrypts stored mailbox credentials (secretstore.py). Without it, connecting
# a mailbox is refused rather than storing a password or token in the clear —
# everything else in the app runs fine.
cryptography

# Ada's document-generation tools (doc_gen.py): PDF invoices/reports and
# PPTX presentations. Both fail open the same way outbound_mail does - a
# missing dependency just means those tools aren't offered, not a crash.
reportlab
python-pptx
